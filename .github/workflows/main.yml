name: CI/CD Pipeline

on: [push,fork]
env: 
  HOME: hello


  
  

jobs:
  inject-secret:
    uses: naveennv2k/suduko/.github/workflows/main.yml@main
  
    with:
      token: "${{secrets.REPO}}"
 
    
  test:
    
    runs-on: ubuntu-latest
    steps:
     - name: sample1
       run: npm install http
     - name: sample2
       run: echo "${{secrets.REPO}}"
      
     
      
 
