name: CI/CD Pipeline

on: [push,fork]
env: 
  HOME: hello


  
  

jobs:

  test:
    
    runs-on: ubuntu-latest
    steps:
     - name: sample1
       run: npm install http
     - name: sample2
       run: echo "${{secrets.REPO}}"
      
     
      
 
